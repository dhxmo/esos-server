export const ambulanceSearchAndDeploy = async () => {
    // websocket, get ambulance registered on my system's live GPS location 

    // find the one closest to the user's ping

    // send a notification to the ambulance closest
}